# Descript.CLI

Command-line interface for the Descript SDK generated with AutoSDK.

## Installation

```bash
dotnet tool install --global Descript.CLI --prerelease
```

## Usage

```bash
descript --help
descript api --help
```